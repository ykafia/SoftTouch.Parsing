namespace MyNamespace.SoftTouch.Hello
{
    
    shader Parent 
    {
        stream int a;
    }

    shader MyShader
    {
        void MyMethod()
        {
            int a = 0;
            int b = (a - 10 / 3 ) * 32 +( streams.color.Normalize()[2] + 2);
        }
    }
}